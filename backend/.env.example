# Required — Claude API key for the agent LLM (claude-opus-5)
ANTHROPIC_API_KEY=

# Phase 3+ — PostgreSQL + pgvector
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/agent

# Phase 6+ — OTLP trace export (Arize Phoenix)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:6006/v1/traces

# PORT=3000
